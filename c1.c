#include<stdio.h>
#include<stdio.h>
int main()
{
int c=0,n,i,j,n1,a;
char s[30][30];
printf("\nenter the number of array");
scanf("%d",&j);
for(i=0;i<n1;i++);
{
scanf("%s",&s[i]);
}
printf("to find number of words inthe length");
scanf("%d",&n);
for(i=0;i<n1;i++)
{
a=strlen(s[i]);
if(a==n)
c++;
}
printf("%d",c);
return 0;
}
